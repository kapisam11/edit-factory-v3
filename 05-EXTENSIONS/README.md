# Extensions and resources

Optional and data-driven resources live here.

- `prompt-library/` — reusable prompt templates
- `learning-data/` — learning/feedback storage
- `hook-scoring/` — hook scoring weights
